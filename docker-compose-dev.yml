version: "3.8"

services:
  server:
    container_name: dev_go_server
    build:
      context: .
      dockerfile: Dockerfile-dev
    ports:
      - ${SERVER_PORT}:${SERVER_PORT}
    volumes:
      - .:/app
      - app_logs:/app_logs
    restart: always

volumes:
  app_logs:
